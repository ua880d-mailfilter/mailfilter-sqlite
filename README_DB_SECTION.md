## Database Setup

A SQLite database is required for rule generation.

You can:

- copy a provided empty schema (recommended)
- let mailfilter create the database automatically
- build a test database using provided .eml datasets

A small synthetic test dataset is included for safe experimentation.
